import { mockLeaves } from '@/data/mockLeaves'; import type { LeaveRequest, LeaveStatus } from '@/types/leave'; export async function getLeaveRequests(){return Promise.resolve(mockLeaves);} export async function updateLeaveStatus(id:string,status:LeaveStatus){const request=mockLeaves.find(item=>item.id===id);if(!request)throw new Error('Leave request not found.');request.status=status;return Promise.resolve(request);}
