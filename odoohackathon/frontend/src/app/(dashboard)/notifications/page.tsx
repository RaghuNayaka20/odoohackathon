import Topbar from '@/components/layout/Topbar'; import { activities } from '@/lib/data'; import Avatar from '@/components/ui/Avatar'; export default function Notifications(){return <><Topbar title="Notifications" eyebrow="Stay in the loop"/><div className="page-width"><section className="panel"><div className="panel-heading"><h2>Recent notifications</h2><span className="text-link">Mark all as read</span></div><div className="activity-list">{activities.map(([name,action,time,initials])=><div className="activity" key={name}><Avatar initials={initials}/><div className="activity-text"><strong>{name}</strong> {action}<small>{time}</small></div></div>)}</div></section></div></>}
