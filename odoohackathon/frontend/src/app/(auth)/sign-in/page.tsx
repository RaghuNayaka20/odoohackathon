'use client';
import { useState } from 'react';
import Link from 'next/link';
export default function SignIn(){const [error,setError]=useState('');return <form className="auth-form" onSubmit={e=>{e.preventDefault();setError('Use the demo workspace to explore Dayflow.')}}><h1>Welcome back</h1><p>Sign in to your Dayflow workspace and pick up where you left off.</p><label className="form-field"><span className="form-label">Work email</span><input className="field" type="email" required placeholder="you@company.com"/></label><label className="form-field"><span className="form-label">Password</span><input className="field" type="password" required placeholder="Enter your password"/></label><div style={{textAlign:'right',margin:'-4px 0 20px'}}><Link href="/forgot-password" className="text-link">Forgot password?</Link></div>{error&&<p style={{color:'#b47639',fontSize:12,margin:'0 0 14px'}}>{error}</p>}<button className="button button-primary" style={{width:'100%'}}>Sign in</button><div className="auth-footer">New to Dayflow? <Link href="/sign-up">Create an account</Link></div></form>}
