'use client'; import { useState } from 'react'; import { Check, X } from 'lucide-react'; export default function ApprovalActions(){const [status,setStatus]=useState('Pending');return status==='Pending'?<div style={{display:'flex',gap:7}}><button className="icon-button" onClick={()=>setStatus('Approved')} aria-label="Approve"><Check size={15}/></button><button className="icon-button" onClick={()=>setStatus('Rejected')} aria-label="Reject"><X size={15}/></button></div>:<span className="eyebrow">{status}</span>}
