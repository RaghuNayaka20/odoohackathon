'use client'; import { useState } from 'react'; export default function LeaveRequestForm(){const [submitted,setSubmitted]=useState(false);return submitted?<p className="form-success" role="status">Your leave request was submitted for approval.</p>:<form onSubmit={event=>{event.preventDefault();setSubmitted(true)}}><label className="form-field"><span className="form-label">Leave type</span><select className="field"><option>Annual leave</option><option>Sick leave</option><option>Personal day</option></select></label><button className="button button-primary">Submit request</button></form>}
