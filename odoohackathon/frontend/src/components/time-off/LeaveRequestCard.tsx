import Badge from '@/components/ui/Badge'; import type { LeaveRequest } from '@/types/leave'; export default function LeaveRequestCard({request}:{request:LeaveRequest}){const tone=request.status==='Approved'?'green':request.status==='Pending'?'amber':request.status==='Rejected'?'gray':'blue';return <article className="panel" style={{padding:17}}><div className="stat-label"><strong style={{color:'var(--ink)'}}>{request.type}</strong><Badge tone={tone}>{request.status}</Badge></div><p style={{fontSize:13,margin:'13px 0 5px'}}>{request.from} - {request.to}</p><small style={{color:'var(--muted)'}}>{request.days} days · {request.reason}</small></article>}
