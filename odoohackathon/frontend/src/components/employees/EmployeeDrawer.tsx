'use client'; import Modal from '@/components/ui/Modal'; import EmployeeProfile from './EmployeeProfile'; import type { Employee } from '@/types/employee'; export default function EmployeeDrawer({employee,open,onClose}:{employee:Employee|null;open:boolean;onClose:()=>void}){return <Modal open={open} onClose={onClose} children={employee ? <EmployeeProfile employee={employee}/> : null}/>}
