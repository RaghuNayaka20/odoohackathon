import Avatar from '@/components/ui/Avatar'; import type { Employee } from '@/types/employee'; export default function EmployeeProfile({employee}:{employee:Employee}){return <div className="panel" style={{padding:22}}><div className="person-cell"><Avatar initials={employee.initials} color={employee.color}/><span><strong>{employee.name}</strong><small>{employee.role} · {employee.department}</small></span></div></div>}
