import Link from 'next/link'; import Avatar from '@/components/ui/Avatar'; import Badge from '@/components/ui/Badge'; import type { Employee } from '@/types/employee'; export default function EmployeeCard({employee}:{employee:Employee}){return <Link href={`/employees/${employee.id}`} className="panel" style={{display:'block',padding:17}}><div className="person-cell"><Avatar initials={employee.initials} color={employee.color}/><span><strong>{employee.name}</strong><small>{employee.role}</small></span></div><div style={{marginTop:14}}><Badge tone={employee.status==='Active'?'green':'amber'}>{employee.status}</Badge></div></Link>}
