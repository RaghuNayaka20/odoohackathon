'use client'; import { useNotifications } from '@/hooks/useNotifications'; import NotificationItem from './NotificationItem'; export default function NotificationPanel(){const {notifications,unreadCount,markRead,markAllRead}=useNotifications();return <section className="panel"><div className="panel-heading"><h2>Notifications {unreadCount>0&&<small className="badge badge-amber">{unreadCount} new</small>}</h2><button className="text-link" onClick={markAllRead}>Mark all read</button></div><div className="activity-list">{notifications.map(item=><NotificationItem key={item.id} notification={item} onRead={()=>markRead(item.id)}/>)}</div></section>}
