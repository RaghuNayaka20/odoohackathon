import { CalendarDays, CircleDollarSign, Clock3, Users } from 'lucide-react'; const stats=[['Total people','48',Users],['Present today','42',Clock3],['On time off','3',CalendarDays],['Payroll this month','$84.2k',CircleDollarSign]]; export default function StatsCards(){return <div className="stats-grid">{stats.map(([label,value,Icon])=><div className="panel stat-card" key={label as string}><div className="stat-label">{label as string}<Icon size={16}/></div><div className="stat-value">{value as string}</div><span className="stat-trend">{label==='Total people'?'+4 this month':label==='Present today'?'87.5% of team':label==='On time off'?'2 requests pending':'Ready to process'}</span></div>)}</div>}
