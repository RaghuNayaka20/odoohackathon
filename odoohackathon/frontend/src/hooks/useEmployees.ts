'use client'; import { useEffect,useState } from 'react'; import { getEmployees } from '@/services/employeeService'; import type { Employee } from '@/types/employee'; export function useEmployees(){const [employees,setEmployees]=useState<Employee[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{getEmployees().then(setEmployees).catch(()=>setError('Unable to load people.')).finally(()=>setLoading(false));},[]);return {employees,loading,error};}
