'use client'; import { useEffect,useState } from 'react'; import { getLeaveRequests } from '@/services/timeOffService'; import type { LeaveRequest } from '@/types/leave'; export function useTimeOff(){const [requests,setRequests]=useState<LeaveRequest[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{getLeaveRequests().then(setRequests).catch(()=>setError('Unable to load leave requests.')).finally(()=>setLoading(false));},[]);return {requests,loading,error,setRequests};}
